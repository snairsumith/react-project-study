export type Product = {
    category: string;
    price: string;
    stocked: boolean;
    name: string;
}

export type ProductListTypes = {
    id: number;
    name: string;
    price: number;
    isStock: boolean;
    description: string;
}